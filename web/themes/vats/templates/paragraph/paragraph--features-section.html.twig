{#
/**
 * @file
 * Theme override for Features Section paragraph.
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--features-section',
    'features-section',
    'py-5',
  ]
%}

{% set heading = paragraph.field_heading.value ?? '' %}
{% set subheading = paragraph.field_subheading.value ?? '' %}

<section{{ attributes.addClass(classes) }}>
  <div class="container container-xl">
    {% if heading %}
      <h2 class="features-section__title text-center mb-3">{{ heading }}</h2>
    {% endif %}

    {% if subheading %}
      <p class="features-section__subtitle text-center mx-auto mb-5">{{ subheading }}</p>
    {% endif %}

    <div class="features-section__grid">
      {{ content.field_features }}
    </div>
  </div>
</section>
